module DayThree where
import Data.Char (isDigit)
import DayOne (dayOneP1)

dayThreeP1 :: [String] -> Int -> Int -> Int
dayThreeP1 lines idx offset
                | lC = 0
                | otherwise = 0
    where
        lC = idx >= length lines